apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dpgchart.fullname" . }}-test-connection"
  labels:
    {{- include "dpgchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dpgchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
